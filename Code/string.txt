%{
    #include <stdio.h>
%}

%%
[A-Z]+[\t\n]   { printf("%s", yytext); }
.              { /* ignore other characters */ }
%%

int yywrap() {
    return 0;
}

int main() {
    printf("Enter the string:\n");
    yylex();
    return 0;
}
